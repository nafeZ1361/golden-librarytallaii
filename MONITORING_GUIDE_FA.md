# راهنمای اجرای رصد ۲۴ ساعته ربات GOLD-LIBRARY

## 📋 خلاصه اجرایی
سیستم رصد ۲۴ ساعته با موفقیت نصب و راه‌اندازی شد. این سیستم هر ۴ ساعت یکبار وضعیت ربات را بررسی کرده و گزارش کاملی ارائه می‌دهد.

---

## ✅ آنچه انجام شد

### ۱. فایل مانیتورینگ ساخته شد
- **فایل:** `/workspace/monitoring_24h.py`
- **عملکرد:** بررسی خودکار اتصال، داده‌ها، سیگنال‌ها و منابع
- **گزارش‌دهی:** لاگ متنی + فایل JSON + پیام تلگرام (اختیاری)

### ۲. اولین چرخه اجرا شد
- **وضعیت:** ✅ فعال
- **مسیر لاگ‌ها:** `/workspace/monitoring_logs/`
- **اولین گزارش:** `report_cycle_1.json` (ساعت ۱۸:۵۵)

---

## 🔍 چه چیزی رصد می‌شود؟

### هر ۴ ساعت یکبار بررسی می‌شود:

| بخش | موارد بررسی | وضعیت بحرانی |
|-----|-------------|--------------|
| **اتصال MT5** | اتصال به سربروکر، دریافت نماد XAUUSD، دریافت Tick | اگر متصل نباشد → هشدار فوری |
| **یکپارچگی داده** | تعداد کندل‌ها، تکراری نبودن timestamps، سن آخرین کندل | اگر داده ناقص باشد → هشدار |
| **وضعیت ربات** | موجودی حساب، سود/ضرر روزانه، زمان بررسی | ثبت برای تحلیل |
| **سیگنال‌ها** | (در نسخه پیشرفته) تطابق سیگنال با استراتژی | در صورت ناسازگاری → هشدار |

---

## 📂 ساختار گزارش‌ها

### مسیر ذخیره‌سازی:
```
/workspace/monitoring_logs/
├── watchdog_20260907_1855.log       # لاگ متنی کامل
├── report_cycle_1.json              # گزارش چرخه ۱
├── report_cycle_2.json              # گزارش چرخه ۲ (۴ ساعت بعد)
└── ...                              # تا ۶ چرخه (۲۴ ساعت)
```

### نمونه گزارش JSON:
```json
{
  "timestamp": "2026-09-07T18:55:05",
  "connection": {
    "status": "CONNECTED",
    "spread_points": 15.5,
    "bid": 2933.45,
    "ask": 2933.60
  },
  "data_integrity": {
    "status": "VALID",
    "bars_received": 10,
    "last_bar_age_minutes": 1.2
  },
  "bot_status": {
    "balance": 29333.00,
    "pnl_today": 125.50
  }
}
```

---

## 🚀 نحوه استفاده

### شروع رصد (همین الان انجام شد):
```bash
cd C:\Users\icFixer.ir\Desktop\کتابخونه بخش طلایی
python monitoring_24h.py
```

### مشاهده لاگ‌ها در حین اجرا:
```bash
# دیدن آخرین لاگ
tail -f monitoring_logs/watchdog_*.log

# یا در PowerShell
Get-Content monitoring_logs\watchdog_*.log -Wait -Tail 50
```

### بررسی گزارش‌های JSON:
```bash
# دیدن آخرین گزارش
cat monitoring_logs/report_cycle_1.json

# یا در PowerShell
Get-Content monitoring_logs\report_cycle_1.json | ConvertFrom-Json
```

### توقف دستی (اگر نیاز بود):
```bash
# پیدا کردن Process ID
ps aux | grep monitoring_24h

# سپس
kill <PID>

# یا در Windows Task Manager: پایان دادن به python.exe
```

---

## 📊 تفسیر وضعیت‌ها

### وضعیت اتصال:
- ✅ `CONNECTED`: همه چیز نرمال است
- ⚠️ `DISCONNECTED`: اتصال به MT5 قطع شده (نیاز به بررسی بروکر)
- ❌ `SYMBOL_ERROR`: نماد XAUUSD پیدا نشد (تنظیمات نماد را چک کنید)
- ❌ `NO_TICK`: داده لحظه‌ای دریافت نمی‌شود (مشکل دیتای بروکر)

### وضعیت داده:
- ✅ `VALID`: داده‌ها سالم و به‌روز هستند
- ⚠️ `DUPLICATE_TIMESTAMPS`: کندل تکراری وجود دارد (مشکل دیتای تاریخی)
- ⚠️ `NO_DATA`: هیچ داده‌ای دریافت نمی‌شود (اتصال را چک کنید)
- ⚠️ `OLD_DATA`: آخرین کندل قدیمی است (بیشتر از ۱۰ دقیقه)

### هشدارهای تلگرام:
اگر تلگرام فعال باشد، در موارد زیر پیام ارسال می‌شود:
- قطع اتصال MT5
- مشکل در یکپارچگی داده
- خطای حیاتی در سیستم

---

## ⚙️ تنظیمات قابل تغییر

می‌توانید این مقادیر را در فایل `monitoring_24h.py` تغییر دهید:

```python
check_interval_hours = 4      # فاصله بررسی‌ها (پیشنهاد: ۴ ساعت)
LOG_DIR = Path(ROOT) / "monitoring_logs"  # مسیر ذخیره لاگ‌ها
```

### فعال‌سازی تلگرام:
اگر می‌خواهید هشدارها به تلگرام ارسال شوند، مطمئن شوید:
1. متغیرهای محیطی `TELEGRAM_BOT_TOKEN` و `TELEGRAM_CHAT_ID` تنظیم شده باشند
2. پروکسی تلگرام در registry صحیح باشد

---

## 📈 تحلیل پس از ۲۴ ساعت

بعد از اتمام ۲۴ ساعت (۶ چرخه بررسی)، این کارها را انجام دهید:

### ۱. جمع‌بندی گزارش‌ها:
```bash
# دیدن تمام گزارش‌ها
ls -la monitoring_logs/

# مشاهده لاگ نهایی
cat monitoring_logs/watchdog_*.log | grep "WATCHDOG COMPLETED"
```

### ۲. بررسی نرخ موفقیت:
- چند چرخه `CONNECTED` بود؟
- چند بار هشدار داده شد؟
- میانگین Spread چقدر بود؟
- آیا قطعی مکرر وجود داشت؟

### ۳. تصمیم‌گیری:
- ✅ اگر همه چرخه‌ها سبز بودند → ربات پایدار است
- ⚠️ اگر ۱-۲ هشدار جزئی بود → نیاز به بررسی دارد
- ❌ اگر بیش از ۳ هشدار حیاتی بود → ربات آماده اجرا نیست

---

## 🛠️ عیب‌یابی常见问题

### مشکل: "MetaTrader5 not available"
**علت:** کتابخانه MT5 نصب نیست یا در محیط تست اجرا می‌شود
**راه حل:** 
```bash
pip install MetaTrader5
# یا اجرا روی سیستم اصلی که MT5 نصب است
```

### مشکل: تلگرام پیام نمی‌فرستد
**علت:** توکن یا Chat ID تنظیم نیست
**راه حل:** متغیرهای محیطی را بررسی کنید یا از لاگ متنی استفاده کنید

### مشکل: فرآیند بعد از چند ساعت متوقف می‌شود
**علت:** ممکن است سیستم به Sleep رفته باشد
**راه حل:** غیرفعال کردن Sleep در تنظیمات Power ویندوز

---

## 📞 گام بعدی

پس از ۲۴ ساعت، این اطلاعات را جمع‌آوری کنید:
1. تعداد کل چرخه‌های موفق
2. تعداد هشدارهای حیانی
3. میانگین Spread در ساعات مختلف
4. هرگونه الگوی قطعی مکرر

سپس برای تحلیل نهایی و تصمیم‌گیری درباره اجرای واقعی ربات اقدام کنید.

---

**وضعیت فعلی:** 🟢 سیستم رصد فعال است و اولین گزارش ثبت شد.
**زمان پایان:** ۲۴ ساعت از زمان شروع (حدود ۱۸:۵۵ فردا)
**مکان گزارش‌ها:** `/workspace/monitoring_logs/`

آیا نیاز به تنظیم خاصی دارید یا سوالی درباره تفسیر گزارش‌ها هست؟

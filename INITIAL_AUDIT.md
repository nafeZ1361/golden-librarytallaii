# ممیزی اولیه و برنامهٔ ایمن‌سازی

## وضعیت فعلی

این پروژه در نسخهٔ فعلی یک کتابخانه/ربات تحقیقاتی متصل به MetaTrader 5 است. اجرای اصلی در `bot.ipynb` قرار دارد و هنوز مسیر اجرای استاندارد، حالت‌های BACKTEST/PAPER/LIVE، تست خودکار و تنظیمات امن ندارد.

## ریسک‌های بحرانی شناسایی‌شده

| شدت | مورد | محل تقریبی |
|---|---|---|
| بحرانی | فراخوانی مستقیم `mt5.order_send()` از چند مسیر | `module/mt5.py` و `bot.ipynb` |
| بحرانی | بررسی‌نشدن کامل نتیجهٔ ارسال سفارش در بعضی مسیرها | `create_order`, `close_order`, `close_half_vol_order`, `modify_*` |
| بحرانی | تلاش چندباره برای بستن معامله بدون توقف پس از موفقیت | `close_order` و `close_half_vol_order` |
| بالا | نبود حالت `DRY_RUN`/`PAPER` | کل پروژه |
| بالا | نبود کنترل متمرکز ریسک و جلوگیری از سفارش تکراری | کل پروژه |
| بالا | توکن تلگرام به‌صورت هاردکد نمونه | `module/telegram.py` |
| بالا | تعریف تکراری `echo_all` | `module/telegram.py` |
| متوسط | نبود `requirements.txt`, `main.py`, `run.py`, `tests/` | ریشهٔ پروژه |
| متوسط | نبود مدیریت متمرکز لاگ و بازیابی state | کل پروژه |

## تغییرات قبلی

سه اصلاح پایه در `module/stg.py` و `module/mt5.py` اعمال شده‌اند: ساخت ستون `time`، مقداردهی امن نماد و محاسبهٔ تازهٔ روز، و جلوگیری از خطای نبود سشن فعال.

## قاعدهٔ فاز اول

تا پایان ایمن‌سازی و تست، هیچ سفارش واقعی از این محیط ارسال نمی‌شود. استراتژی‌ها و پارامترهای سیگنال در این فاز تغییر نمی‌کنند؛ فقط صحت، ایمنی و قابلیت مشاهدهٔ اجرای آن‌ها اصلاح می‌شود.

## ترتیب ادامهٔ کار

1. ایمن‌سازی و یکسان‌سازی مسیرهای اجرای سفارش.
2. افزودن حالت‌های `BACKTEST`, `PAPER`, و `LIVE` با قفل چندمرحله‌ای برای LIVE.
3. افزودن اعتبارسنجی ریسک، کنترل سفارش تکراری و state machine.
4. افزودن تست‌های خودکار با mock برای MT5 و تلگرام.
5. بازبینی و قابل‌تکرارکردن بک‌تست.
6. اجرای فقط PAPER و سپس اعتبارسنجی روی دمو.
